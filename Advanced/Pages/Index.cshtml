@page "/pages"
@using Advanced.Blazor
@model IndexModel
@{
    ViewData["Title"] = "Работники";
}

<h4 class="text-center">@ViewBag.Title</h4>

<component type="typeof(PeopleList)" render-mode="Server"/>
