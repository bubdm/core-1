<table class="table">
    <thead>
    <tr>
        <th>№</th>
        <th>Фамилия</th>
        <th>Имя</th>
        <th>Отчество</th>
        <th>Возраст</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var person in Persons)
    {
        <tr class="@GetClassPerson(person.Age)">
            <td>@person.Id</td>
            <td>@person.LastName</td>
            <td>@person.FirstName</td>
            <td>@person.Patronymic</td>
            <td>@person.Age</td>
        </tr>
    }
    </tbody>
</table>
<div class="form-group">
    <h3>Обзор сотрудников по возрасту</h3>
    <p>Выделение сотрудников с возрастом более, лет:</p>
    <input type="number" @bind-value="SelectedAge" @bind-value:event="oninput"/>
</div>
